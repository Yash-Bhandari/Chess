package helper;

import java.util.ArrayList;

public final class Move {
    ArrayList moves;

    public Move(Position ... startEnds) {
        assert startEnds.length % 2 == 0;
        for (int i = 0; i < startEnds.length; i += 2) {
        }
    }

}
